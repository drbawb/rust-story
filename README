So I got a little !(sober) and decided it'd be a good idea
to attempt the first bits of [reconstructing cavestory](#slug) in `Rust`...

Why Rust?

BECAUSE PARADIGMS.

---

Structure:
-- rust-story
	-- src: 	Main source files used by rust-story
	-- lib: 	Shared libraries used by rust-story
	-- assets:	Game assets, taken from `reconstructing-cavestory` / freeware version of cavestory


